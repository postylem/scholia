# v0.3 Plan: Packaging for Cross-Directory Use

**Status:** Stub — to be fleshed out in a future session.

**Goal:** Package scholia so it can be invoked from any working directory to annotate
documents across different projects.

## Open Questions

- Install method: `pipx install scholia` (isolated), `uv tool install`, or just `pip install --user`?
- Should `scholia start` accept absolute paths, or should it resolve relative to cwd?
- Config file location: `~/.config/scholia/config.toml` for global settings (username, theme, default port)?
- How to handle per-project `.scholiarc` or similar for project-specific settings?
- Port conflict resolution when running multiple scholia instances on different docs

## Likely Tasks

1. Ensure `pyproject.toml` entry point works globally after install
2. Add global config support (`~/.config/scholia/`)
3. Handle path resolution for cross-directory invocation
4. Test `pipx install .` workflow
5. Add `--port` flag for multi-instance use
